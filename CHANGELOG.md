# 更新日志

本文件记录可交接的功能、修复、构建和验证结果。日期以仓库提交日期为准。

## 2026-07-20

### AI 内部测试与工作台

- 新增独立 `AI 内部测试` Tab，不影响既有登录、对标账号、作品采集、评论筛选和 Excel 导出流程。
- 新增智能体工作台：测试总览、增量巡检、线索筛选和机会排序四个可辨识的导航按钮；未选中项也提供边框、填充、悬停和键盘焦点反馈，避免被误认为普通说明文字。
- 新增 `pipeline/lead_ai.py`：先筛选再机会排序，机会阶段只接收筛选保留项；严格校验 JSON 字段、枚举、长度、`lead_id` 和置信度，低于 0.65 强制人工复核。
- AI 分析限制为人工选择的最多 50 条已入库评论；结果只写入 `ai.screening`、`ai.opportunity` 与兼容标签，不自动私信、不修改人工联系状态。
- 新增本机每日增量巡检：强制刷新对标账号，只采集新增作品或评论数增长的非零评论作品；软件未运行时不会在后台执行。
- 增加房产场景业务上下文草案、V3 提示词、结构化输出正反例和输入输出契约文档。
- 本地 `ai_config.json`、`comment_leads.json`、`short_video_profiles.json` 纳入 Git 忽略，避免提交 API 密钥、用户数据或作品快照。
- 回归验证：`78 passed`。

### 工作区未打包更新

- 通用安装包 `release/LeadShrimpSetup_0.1.6.exe` 已生成；Inno 编译、安全扫描和 staging 健康检查通过。本次包使用本机 Edge 运行时，未等待网络下载内置 Chromium。
- 主页作品采集数量默认 5 条，支持调整为 10 或 20 条；前后端统一限制最高 20 条。
- 评论数为 0 的作品继续展示在选择页，但卡片明确标注“评论为 0”；用户选中后会在前端阻止采集并提示刷新。新增“刷新作品信息”按钮，可对当前竞品账号强制刷新。
- 排查评论采集“无法打开 Edge 或 Chromium”问题：通用构建现在会把 Playwright Chromium 一并放入安装包，运行时优先使用 Edge，并增加 Edge 常见安装路径兜底；浏览器启动失败会保留每次尝试的具体原因。
- 选择作品页主按钮改名为“采集评论”，移动到作品列表下方；点击后显示动态采集进度、选中作品数和评论上限，完成或失败时显示明确结果。
- 选择作品页新增“按竞品账号查看作品”筛选，切换账号时优先展示该账号已缓存作品，并在作品卡片显示所属账号。
- 竞品账号列表的“采集作品”继续作为一键刷新入口；刷新后回到选择作品页，便于立即勾选评论采集范围。
- 作品快照刷新时按作品 ID 对比上次缓存：新出现的作品显示左上角黑色半透明“新视频”标记。
- 当前评论数高于上次缓存时，作品封面右上角显示红色 `+数量` 标记。
- 新增回归测试覆盖快照标记与竞品账号筛选；本次未重新生成安装包。

## 2026-07-16

### 中文客户线索 Excel 导出 0.1.5

- 导出改为 `.xlsx`，只保留评论时间、评论人、评论内容、评论人 IP、评论人主页、联系状态 6 列。
- 设置中文表头、冻结首行、自动筛选、合理列宽、评论内容自动换行和行高。
- 安装包：`release/LeadShrimpSetup_0.1.5.exe`。

### 通用安装包 0.1.4

- 重新生成无需商业授权、账号系统、加密和运行时完整性校验的通用 Inno 安装包。
- 安装包内置 Python、Playwright 和项目依赖，安装后直接启动本地 Web 页面。
- 文件：`release/LeadShrimpSetup_0.1.4.exe`，约 62 MB。

### 监控删除与登录窗口体验 0.1.3

- 监控列表新增删除入口和 `DELETE /api/comment-leads/monitors/{id}` 接口；删除监控对象时保留历史评论线索，仅清理该对象的待执行任务。
- 工作流程点击“登录抖音”前先检查现有登录态；已登录时提示“抖音已登录，无需重复打开登录窗口”，不再反复拉起浏览器。
- 未登录时打开抖音后自动尝试触发登录面板并选择“扫码登录”；若入口延迟渲染，则通过搜索框交互刷新页面后再次查找。
- 修复登录时共享 Cookie 覆盖持久化浏览器会话的问题：已有 Profile 登录态时不再重复注入 Cookie，仅为空 Profile 提供初始化缓存。
- 登录浏览器固定恢复到可见位置和尺寸；评论采集继续使用屏幕外窗口，命中验证时才带回前台供用户处理。
- 删除、登录短路、扫码触发、会话保护和窗口参数补充回归测试；本次验证：`61 passed`。

### 中文安装与启动提示 0.1.2

- 通用 Inno 安装器改用项目内置简体中文语言包，不依赖本机 Inno Setup 的语言文件。
- 启动失败页面改为中文说明，完整 Python traceback 仅写入本地日志。
- 重新生成：`release/LeadShrimpSetup_0.1.2.exe`。

### 登录等待与安装包 0.1.1

- 登录浏览器默认持续等待，不再因 30 秒、180 秒或 240 秒超时返回 503。
- 只有检测到有效抖音登录态后，登录请求才会成功返回；浏览器异常退出仍会返回错误。
- 普通采集任务继续保留进程超时保护，避免采集异常无限占用服务。
- 重新生成通用安装包：`release/LeadShrimpSetup_0.1.1.exe`。
- 本次验证：`53 passed`，staging 安全扫描通过，Inno Setup 编译成功。

## 2026-07-15

### 评论范围滑块

- 将评论滑动选择改为左侧单一竖向轨道和圆形滑块。
- 滑块范围外的当前结果会同步取消选择，避免历史勾选残留。
- 放大轨道命中区域和滑块尺寸，降低拖动误差。

### 安装启动稳定性

- 修复 `pythonw.exe` 启动时 Uvicorn 默认日志格式调用 `sys.stdout.isatty()` 导致服务在绑定端口前退出的问题。
- 启动器不再固定把浏览器打开到 `8922`：端口被占用时会自动顺延选择可用端口。
- 启动器会等待独立 `/api/health` 返回成功后再打开前端，避免服务尚未拉起时出现 `ERR_CONNECTION_REFUSED`。
- 服务启动超时或端口全部不可用时，会生成 `%LOCALAPPDATA%\LeadShrimp\logs\launcher.log` 和 `startup-error.html`，便于远程排错。
- 捕获 `pythonw.exe` 下的模块导入异常和 Uvicorn 启动异常，写入完整 traceback 后打开诊断页。

### 通用版安装包

- 新增 `build_public_release.ps1`：复制 Python 运行时、安装依赖、执行安全扫描并调用 Inno Setup。
- 新增 `lead_shrimp_public.iss`：安装后通过内置 `pythonw.exe -m lead_shrimp.launcher` 启动本地 Web 应用。
- 新增 `一键打包通用版.bat`。
- 通用版不启用加密、账号系统和远端卡密；商业版构建仍单独保留。
- Inno 生成 `release/LeadShrimpSetup_0.1.0.exe`，本次实测约 62 MB。
- 修复发布扫描将 Python 标准库误判为密钥、将运行时缓存目录误判为开发产物的问题；业务源码和用户数据仍继续扫描。

### 采集和缓存

- 主页作品缓存可在刷新后恢复，缓存超过一天时提示可能有新作品或新数据。
- 作品选择显示发布时间、点赞数、评论数和置顶状态，并支持按发布时间筛选。
- 支持每条作品设置评论采集上限：100、300、500、1000、2000。
- 评论支持最新评论/最早评论排序、关键词、日期范围和时间范围筛选。
- 排除作品作者自己的评论，按稳定评论 ID 去重。
- 支持选中评论导出、当前结果全选、按日期加入选择和滑动连续选择。

### 浏览器与登录

- 登录态写入独立的 LeadShrimp 数据目录，刷新前端不会要求重复激活/登录。
- 采集浏览器尽量后台运行，验证页或登录页才让用户介入。
- 修复同步 Playwright API 在异步事件循环中的调用错误，采集逻辑使用异步 API。
- 采集浏览器保持后台生命周期，减少反复打开窗口和抢占用户焦点。

### 验证

- 产品、授权服务和构建契约测试：`38 passed`。
- 通用版 staging 安全扫描通过。
- 内置运行时导入检查通过：`fastapi`、`playwright`、`lead_shrimp.app`。

## 仍需验证

- 真实抖音账号下作品发布时间是否在不同页面状态都能返回。
- 评论分页是否能达到用户选择的上限，及平台限制时的部分完成提示。
- 安装包在没有系统 Python 的干净 Windows 上启动、升级和卸载。
- Microsoft Edge 缺失或版本不匹配时的用户提示。
