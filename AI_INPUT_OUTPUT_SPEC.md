# 获客虾 AI 智能体输入输出规格

更新时间：2026-07-17

## 1. 目标与边界

AI 的职责是协助客服从**已采集的公开评论**中发现值得跟进的需求，不负责抓取、绕过平台验证、私信、关注、发布内容或推断手机号等非公开信息。

系统采用两阶段，而不是让一个模型同时做所有事：

```text
公开评论入库
  -> 筛选智能体：清洗、判断是否保留、打意图标签
  -> 机会智能体：仅处理保留线索，给出优先级与跟进建议
  -> 客服确认、联系、记录状态
  -> 后续可用结果做复盘
```

这样前端可以稳定展示，客服也能区分“为什么留下”与“下一步怎么跟”。

## 2. 当前项目真实字段

评论采集模块 `pipeline/comment_leads.py` 当前已保存下列字段。AI 输入只能使用这些字段或后续明确新增的业务配置，不能要求模型自行补全缺失信息。

| 业务含义 | 当前字段 | 使用规则 |
| --- | --- | --- |
| 线索 ID | `lead_id` | 仅用于回写定位，不传给模型作判断依据 |
| 评论 ID | `comment_id` | 去重与审计使用，不传给模型 |
| 评论内容 | `content` | 核心判断依据 |
| 评论时间 | `create_time` | 判断时效性 |
| IP 属地 | `comment_ip_location` | 仅在业务地域匹配时参考，不能据此推断身份或敏感特征 |
| 评论人昵称 | `commenter_nickname` | 仅辅助理解语境 |
| 主页签名 | `commenter_signature` | 可选辅助信息，不得臆测个人身份 |
| 评论人主页 | `commenter_profile_url` | 仅用于客服人工打开 |
| 评论点赞/回复 | `like_count` / `reply_count` | 可选互动热度信号 |
| 来源作品 | `source_url` | 溯源使用 |
| 对标账号 ID | `monitor_id` | 用于加载该账号的业务备注 |
| 当前状态 | `status` | 例如“待联系”，不得被 AI 擅自改写 |
| 已有标签 | `ai_label` | 过渡兼容字段，未来由 `screening.intent_tags` 汇总写入 |

作品标题、发布时间、点赞数、评论总数保存在监控对象的 `cached_videos` 快照中。调用 AI 时，服务端应按 `source_url` 或作品 ID 关联后再传入；关联失败则传空值，不能编造。

## 3. 业务配置：人工先填写，AI 后判断

每个对标账号或客户项目新增一份可编辑配置。没有此配置时，AI 只能做通用意图识别，不能判断“高价值”。

```json
{
  "business_name": "示例：昆明本地装修",
  "offer": "全屋整装、旧房改造、量房咨询",
  "target_locations": ["昆明", "云南"],
  "target_customer": "近期有装修、购房或局改需求的本地家庭",
  "high_intent_signals": ["多少钱", "怎么收费", "地址", "预约", "想了解", "能来量房吗"],
  "exclude_signals": ["纯表情", "无关闲聊", "同行招募", "辱骂广告"],
  "contact_style": "专业、简短、不夸大；先回答问题再邀请进一步沟通",
  "forbidden_claims": ["保证最低价", "虚构优惠", "索要手机号"],
  "version": "2026-07-17"
}
```

该配置由运营或负责人确认，作为 AI 的唯一业务上下文。提示词、输出与每次结果都应记录 `version`，方便复盘“是模型变了还是业务规则变了”。

## 4. 筛选智能体

### 输入

一次处理 20 至 50 条评论，降低成本且方便失败重试。输入格式固定如下：

```json
{
  "task": "screen_comment_leads",
  "business_context": {"...": "见第 3 节"},
  "leads": [
    {
      "lead_id": "765451...",
      "content": "请问有清真食堂吗",
      "create_time": "2026-07-10T10:20:30+08:00",
      "comment_ip_location": "云南",
      "commenter_nickname": "暴躁小无赖",
      "commenter_signature": "",
      "like_count": 0,
      "reply_count": 0,
      "video": {
        "title": "作品标题，可为空",
        "publish_time": "2026-07-09T12:00:00+08:00",
        "like_count": 274,
        "comment_count": 154
      }
    }
  ]
}
```

### 系统提示词骨架

```text
你是获客虾的评论线索筛选助手。只根据提供的业务配置与评论事实判断，
不得编造用户身份、购买力、联系方式、历史行为或作品未提供的信息。

对每个 lead_id 返回且只返回一个 JSON 对象。保留“明确需求、价格/地址/预约
咨询、适配业务的求助、可人工确认的潜在需求”；排除纯表情、无关闲聊、广告、
明显与业务无关内容。模糊内容可保留为低意向，但必须降低置信度。

输出必须严格符合指定 JSON Schema；不得输出 Markdown、解释文字或额外字段。
```

### 输出 Schema

```json
{
  "schema_version": "1.0",
  "results": [
    {
      "lead_id": "765451...",
      "keep": true,
      "intent_level": "high",
      "intent_tags": ["询价", "本地需求"],
      "summary": "询问服务价格，存在明确咨询意图。",
      "reason": "评论直接询问收费，且 IP 属地与目标地域匹配。",
      "confidence": 0.92,
      "needs_human_review": false
    }
  ]
}
```

约束：

- `intent_level` 只能为 `high`、`medium`、`low`、`exclude`。`high` 表示明确房源决策或行动，`medium` 表示明确居住决策问题，`low` 表示有教育/生活/通勤等隐性置业信号但尚未确认购房计划；它们都不是成交承诺。
- `confidence` 为 0 到 1 的数字；低于 0.65 必须 `needs_human_review: true`。
- `intent_tags` 最多 3 个，每个不超过 8 个中文字符。
- `summary` 不超过 40 个中文字符；`reason` 不超过 80 个中文字符。
- `keep: false` 时，`intent_level` 必须为 `exclude`，且不得产生跟进建议。

### 回写结构

不覆盖评论原字段，只新增：

```json
{
  "ai": {
    "screening": {
      "schema_version": "1.0",
      "model": "模型标识",
      "prompt_version": "screening-2026-07-17-v1",
      "business_config_version": "2026-07-17",
      "processed_at": "2026-07-17T10:30:00+08:00",
      "keep": true,
      "intent_level": "high",
      "intent_tags": ["询价", "本地需求"],
      "summary": "询问服务价格，存在明确咨询意图。",
      "reason": "评论直接询问收费，且 IP 属地与目标地域匹配。",
      "confidence": 0.92,
      "needs_human_review": false
    }
  },
  "ai_label": "高意向·询价"
}
```

`ai_label` 只用于兼容当前前端标签；新前端读取 `ai.screening` 展示标签、理由和置信度。

## 5. 机会智能体

机会智能体只接收 `screening.keep=true` 的评论。它不是重复做筛选，而是基于业务目标决定客服资源该先投向哪里。教育、学校、健身房、公园、医院、商场、通勤等问题，只要处于楼盘/居住语境中，均可作为潜在置业线索进入本阶段；优先级应由证据强度决定，不能因为没有直接说“买房”而丢弃。

### 输入与输出

输入包含原始事实、筛选结果和业务配置。输出不能更改 `keep`，只能给优先级和建议：

```json
{
  "schema_version": "1.0",
  "results": [
    {
      "lead_id": "765451...",
      "priority": "P1",
      "follow_up_channel": "抖音私信",
      "recommended_action": "优先在 24 小时内人工私信答疑",
      "opening_message": "看到你在评论区咨询价格，我们可以先根据你的需求给你做个大致方案。",
      "rationale": "明确询价，且评论时间较新。",
      "risks": ["需先确认具体需求，不能直接报价"],
      "confidence": 0.86,
      "needs_human_review": false
    }
  ]
}
```

约束：

- `priority` 只能为 `P1`、`P2`、`P3`；P1 是优先处理，不等于“必然成交”。教育或生活配套型隐性需求通常从 P2/P3 开始，只有同时出现明确购房、询价、房源、看房等行动证据才可升至 P1。
- `follow_up_channel` 当前只能为 `抖音私信`、`人工查看主页`、`暂不跟进`。
- `opening_message` 不超过 60 个中文字符，不能索要手机号、不能承诺无法证明的权益、不能假装已经了解对方。涉及学校/入学时，只能提示可协助核实周边教育配套或当年政策，不得暗示购房即可保证入学。
- 无法判断时，输出 P3 + `needs_human_review: true`，不能强行给 P1。

回写位置为 `ai.opportunity`，保留模型、提示词和业务配置版本，便于重新跑模型而不污染人工跟进状态 `status`。

## 6. 前端展示与人工控制

第一版只增加三处，不把评论工作台做复杂：

| 位置 | 展示内容 | 人工动作 |
| --- | --- | --- |
| 评论线索弹窗 | `P1/P2/P3`、意向标签、简短理由 | 查看、勾选、打开主页 |
| 线索详情抽屉 | AI 判断依据、建议首句、风险提示、模型版本 | 采纳/忽略/重新分析 |
| 批量操作栏 | “AI 筛选选中评论” | 指定业务配置后执行 |

人工状态仍以 `待联系`、`已私信`、`无效`、`已成交` 等为准。AI 不得自动发送私信或自动把线索标记为已联系。

## 7. 失败、校验与成本控制

1. 服务端按 JSON Schema 校验每条结果；不合法、缺少 `lead_id` 或返回未知 ID 的结果一律丢弃并记录错误。
2. 单条失败不影响已校验通过的其他评论；失败评论显示“待 AI 重试”，不改人工状态。
3. 模型超时、限流、网络失败使用指数退避重试，最多 3 次；之后进入待重试队列。
4. 相同 `lead_id + 评论内容 + business_config_version + prompt_version` 命中结果缓存，不重复消耗模型调用；评论编辑或业务配置变更后自动失效。
5. 优先使用规则过滤无效评论，再把候选评论交给模型；可显著降低成本并提升稳定性。
6. 保存最小必要数据：不要向模型提供 cookie、登录态、设备信息、内部路径或不需要的完整用户资料。
7. 所有 AI 输出必须可追溯：保存模型名、提示词版本、配置版本、处理时间与原始响应摘要。

## 8. 推荐实施顺序

1. 新增业务配置编辑页和本地 `ai_business_configs.json`。
2. 新增 `ai.screening` 的数据结构、批处理服务和 Schema 校验，但暂不自动执行。
3. 在评论线索弹窗加入“AI 筛选选中评论”，展示结果与人工复核。
4. 累积 100 至 300 条人工确认样本，校准标签、阈值和提示词。
5. 再接入机会智能体、优先级队列与客服跟进建议。
6. 最后才接入每日巡检调度；巡检只负责发现变化和创建任务，AI 仍需经过上述可审计流程。

## 9. 交给外部 ChatGPT 协作的材料

将第 3、4、5 节连同 20 至 50 条**脱敏真实评论样本**交给 ChatGPT，请它只优化：

- 意向标签词表与排除规则；
- 筛选/机会两份系统提示词；
- P1/P2/P3 的判定阈值；
- 结构化 JSON 的示例与反例。

不要让外部模型自行设计接口、字段或前端；这些必须以本文件为准。得到建议后，由项目内实现统一转换、校验和版本化。
